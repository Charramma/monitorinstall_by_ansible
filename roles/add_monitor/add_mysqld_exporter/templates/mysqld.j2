[
    {
        "targets": [
            "{{ hostvars[item].ansible_ssh_host }}:{{ hostvars[item].mysqld_exporter_port }}"
        ],
        "labels": {
            "instance": "{{ hostvars[item].inventory_hostname }}",
            "service": "mysql"
        }
    }
]
