[
    {
        "targets": [
            "{{ hostvars[item].ansible_ssh_host }}:{{ hostvars[item].zookeeper_exporter_port }}"
        ],
        "labels": {
            "instance": "{{ hostvars[item].inventory_hostname }}",
            "service": "zookeeper"
        }
    }
]
