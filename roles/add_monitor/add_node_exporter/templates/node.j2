[
    {
        "targets": [
            "{{ hostvars[item].ansible_ssh_host }}:{{ hostvars[item].node_exporter_port }}"
        ],
        "labels": {
            "instance": "{{ hostvars[item].inventory_hostname }}",
            "service": "node-service"
        }
    }
]
